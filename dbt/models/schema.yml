sources:
  - name: e_commerce_dataset
    tables:
      - name: e-commerce-data

models:
  - name: dim_customers
    description: >
      Generated artificial data about customer
    columns:
      - name: user_id
        tests:
          - unique
          - not_null
      - name: state
